<a href="{{ path('ability_show', {'code': code, 'slug': ability.slug}) }}"
   title="{{ ability.description|capitalize }}"
   id="pokemon-sheet-current-ability"
>
    {{ ability.name|capitalize }}
</a>
