<table class="table table-striped profile-table">
    <tbody>
    {% for description in arrayDescriptionVersion %}
        <tr>
            <th>{{ description.version.name }}</th>
            <td>
                {{ description.description }}
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
