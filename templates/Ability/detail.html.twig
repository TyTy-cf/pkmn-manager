{% extends 'base.html.twig' %}

{% block body %}

    <div class="container-md">
        <h1 class="text-center mb-1 mt-3">{{ ability.name }}</h1>
        <hr>
        <div class="grid-row">
            <div class="grid-col ">
                <h2>{{ 'index.ability.name'|trans }}</h2>
                <p>{{ ability.description }}</p>
            </div>
            <div class="grid-col ">
                <h2>{{ 'index.ability.pokemons'|trans }}</h2>
                <div>
                    {% include 'Pokemon/table-pokemon.html.twig' %}
                </div>
            </div>
        </div>
    </div>

{% endblock %}